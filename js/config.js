// Configuration settings
const config = {
    // Admin credentials
    adminCredentials: {
        username: 'admin@doubleact.com.au',
        password: 'Fireb1rd01!'
    }
};

export default config;
